# ZotNotes 全量修复计划（只读审查后的落地实施）

> 本文件是实施计划的存档副本（2026-08-08 执行），供回溯参考。

## Context

对 `zotnotes_tool` 做了全量只读审查（6 个源码文件全部读完），发现 1 个功能失效 bug、1 个库大时静默截断的 bug，以及若干冗余/死代码。本次把审查发现**全部修完**；exe 重打包**本次不做**（源码稳定后再单独做）。改动只涉及 4 个源码文件 + 新增本计划文档，不改依赖、不改配置默认值、不改 UI 布局，多阶段管道与 `generate_note` 返回值结构保持不变。

## 修复项总览

| # | 类型 | 位置 | 修复 |
|---|---|---|---|
| 1 | bug | note_generator.py `generate_note` | 「启用 LLM」开关失效，`render_note` 死代码接回 |
| 2 | bug | zotero_client.py `fetch_papers` | Zotero 分页缺失，>100 篇静默截断 |
| 3 | bug | gui/app.py `generate_one`/`generate` | 「需原文」骨架笔记无法重试 |
| 4 | bug | obsidian_writer.py `_scan` | 占位符检测只读前 2000 字符 |
| 5 | bug | note_generator.py `_extract_figures` | `doc.close()` 异常时泄漏 + 暂存目录永不清理 |
| 6 | bug | note_generator.py `_placeholder_vals` | YAML 转义缺失（Windows 反斜杠路径、引号） |
| 7 | 冗余 | app.py `_refresh_worker`/`_refresh_info` | 一次刷新扫 7 遍笔记目录 |
| 8 | 冗余 | app.py `_gen_one`/`_generate_one_worker` | 近重复，合并 |
| 9 | 冗余 | note_generator.py `render_skeleton` | 手写 frontmatter 与 DEFAULT_TEMPLATE 双份维护 |
| 10 | 冗余 | note_generator.py `_strip_fences`/`_extract_json` | 围栏剥离逻辑重复 |
| 11 | 死代码 | zotero_client.py | 局部 import 冗余、`key += "Paper"` 不可达分支 |

## 一、obsidian_writer.py

### 1a（Bug#4）`_scan` 读全文 + 占位符限定在 LLM 正文区
- `_scan` 里 `head = f.read(2000)` 改为 `content = f.read()`。
- 占位符检测**只对 LLM 正文区**计数：`body = content.split("## 我的笔记", 1)[0]`，对该切片跑 `PLACEHOLDER_HITS_RE.findall`。理由：用户手写的「我的笔记/疑问」区若出现 2 次 TODO 会被误判为「需修复」，批量时被默认重写（破坏手写内容）。限定在 LLM 区可消除误判。
- `FRONTMATTER_KEY_RE` / `EVIDENCE_INSUFFICIENT_RE` 仍在全文搜（frontmatter 在顶部，语义不变）。

### 1b（Bug#7 配套）`_scan` → 公开 `scan_states()`
- `_scan` 改名 `scan_states()`（无外部调用，仅内部用）。
- 4 个查询方法 `existing_note_keys` / `insufficient_note_keys` / `placeholder_note_keys` / `note_mtimes` 改为 `self.scan_states()` 的派生（签名保留——`test_core.py:27` 用 `existing_note_keys`）。

## 二、note_generator.py

### 2a（Bug#6）YAML 转义
- 新增模块级函数 `_yaml_escape(s)`：`s.replace("\\","\\\\").replace('"','\\"')`。
- `_placeholder_vals`：`zotero_key/title/authors/year/doi/url/item_type/pdf/note_file` 统一套 `_yaml_escape`；tags 逐个转义再拼引号；新增 `"aliases"` key = `'"<escaped title>"'`；`llm` 保持 `""` 不动。
- 连带修复：`pdf` 字段是 `C:\Users\...`（反斜杠），`\U`/`\f` 是非法 YAML 转义，当前 frontmatter 的 pdf 行就是坏的。

### 2b（Bug#9）抽 `_frontmatter` 共享
- 在 `DEFAULT_TEMPLATE` 定义**之前**新增 `FM_ORDER` 常量 + `_frontmatter(vals, extra=None)`。
- `DEFAULT_TEMPLATE` 改为 `_default_template()` 生成，**与原字面量逐字节一致**（含结尾 `## 疑问\n`）。
- `render_skeleton` frontmatter 改用 `_frontmatter(vals, "evidence: insufficient")`。
- 以后加字段只改 `FM_ORDER` + `_placeholder_vals` 两处。

### 2c（Bug#10）`_extract_json` 复用 `_strip_fences`
- `_extract_json` 开头 `t = _strip_fences(text)`，删掉手写围栏剥离块，保留 `find("{")/rfind("}")` 切片。

### 2d（Bug#5）`_extract_figures` try/finally + 暂存清理
- `_extract_figures` 用 `doc = None; try: ...; finally: if doc: doc.close()`。
- **注意**：原 `doc.close()` 位于 `for pno` 循环**之后**（位置正确），修复点是保证循环中途异常时也 close。
- 空目录当场清：`if not figs: shutil.rmtree(staging, ignore_errors=True)`。
- 新增幂等 `cleanup_figures(figures)`：删除图表暂存目录，调用方在拷贝到 vault 后调用。

### 2e（Bug#1）`llm_enabled` 检查 + `render_note` 接回
- `generate_note` 开头（PDF 提取之前）插入：
  ```python
  if not cfg.get("llm_enabled", True):
      return {"content": render_note(paper, cfg, note_file), "figures": [], "status": "ok"}
  ```
- LLM 阶段（`_build_bundle` 到 `_substitute` 前）包 try/except：异常时 `cleanup_figures(figures)` 后 `raise`。
- 同步更新 `generate_note` 与 `render_note` docstring。

## 三、zotero_client.py

### 3a（Bug#2）分页
- `_get` 拆出 `_get_resp(path) -> requests.Response`（含原超时/状态码处理），`_get` 改为 `return self._get_resp(path).json()`。
- `fetch_papers` 改为翻页循环：`start`（0 基偏移）+ `limit=100`，读 `Total-Results` 头；终止条件三重保险（`start >= total` / `len(page) < PAGE_SIZE` / `start >= MAX_ITEMS`），空库立即 break。
- 附带修复：`by_key` 覆盖全库，第 2 页之后的 PDF 附件也能被 `_find_pdf` 识别。

### 3b（Bug#11）死代码清理
- `_pdf_path` 删局部 `from urllib.parse import unquote, urlparse`，改用 `urllib.parse.urlparse/unquote`（顶部已 import）。
- `fallback_citation_key` 删 `if len(key) < 3: key += "Paper"`（不可达：base 最短为 "Unknown" 7 字符）。

## 四、gui/app.py

### 4a（Bug#3）`needs_source` 可直接重生成
- 单篇：`if row.note_state in ("ok","needs_source") and not overwrite` → `if row.note_state == "ok" and not overwrite`。
- 批量：`to_process = [r for r in selected if overwrite or r.note_state != "ok"]`；`skips = [r for r in selected if r.note_state == "ok" and not overwrite]`。

### 4b（Bug#8）合并生成入口 + 暂存清理
- 新增 `_run_generate(row, writer)`：generate_note → write_note → import_images → cleanup_figures，返回 `(ok, status, msg)`。
- `_generate_one_worker` 改为调 `_run_generate(row, self.writer)`；删除 `_gen_one`；批量 `pool.submit(self._run_generate, r, writer)`。
- 顶部 import 加 `cleanup_figures`。

### 4c（Bug#7）单次扫描
- `_refresh_info(states=None)`：states 为 None 时 `self.writer.scan_states()`，一次派生 done/need/bad。
- `_refresh_worker`：`states = self.writer.scan_states()`（一次），`result = (papers, states)`。
- `_apply_refresh`：解包 2 元组，从 states 派生 note_keys/insufficient/placeholders/mtimes，空库与正常分支都传 states 给 `_refresh_info`。
- 效果：刷新路径扫 1 次（原 4+3=7）；生成后 `_finish_*` 无参调 `_refresh_info` 各重扫 1 次（合理）。

## 实施顺序

1. obsidian_writer.py（1a → 1b）
2. note_generator.py（2a → 2b → 2c → 2d → 2e）
3. zotero_client.py（3a → 3b）
4. gui/app.py（4a → 4c → 4b）
5. 写本计划文档

## 验证

1. **字节一致性**：2b 改完后确认 `DEFAULT_TEMPLATE` 生成结果与原字面量逐字节一致（含换行）。
2. **静态检查**：改完各文件跑 `python -m py_compile`。
3. **test_core.py**（只读自检，需 Zotero 运行）：确认 `existing_note_keys` 等兼容性。
4. **test_pipeline.py**（端到端，需 Zotero + 有效 API Key）：确认 `generate_note` 返回结构不变、`_extract_figures` 正常。
5. **手动冒烟**（GUI）：关 LLM 出模板不调接口；开 LLM 各分区齐全无占位符；刷新 >100 篇全显示；「需原文」可重试；frontmatter 的 `pdf:` 行 YAML 合法。
6. **回归**：生成成功路径下 `images/<citekey>/` 图片正常、`%TEMP%\zotnotes_fig_*` 目录不再残留。
