# ZotNotes 使用说明

## 首次使用

1. 启动 Zotero 7，在设置中允许本机其他程序通信，然后重启 Zotero。
2. 运行 `ZotNotes.exe` 或 `python main.py`。
3. 点“设置”：
   - “输出文件夹”直接选择最终保存论文笔记的目录；
   - “笔记模板”通常保持默认；
   - 填写 OpenAI 兼容接口地址、API Key 和模型名；
   - “研究领域 / 写作偏好”可以留空，也可以写自己的领域和偏好。
4. 保存后刷新文献。

程序不再提供“高级设置”。Zotero 本地地址固定为标准地址；有 PDF 时始终读取正文；生成按钮同时承担首次生成和重新生成；是否显示无 PDF 文献由主界面的筛选按钮控制。

## 主界面

- 搜索：按标题、作者或年份查找。
- 含 PDF：默认视图，只看可读取原文的条目。
- 全部：包括无 PDF 条目。
- 未生成：只看尚无笔记的条目。
- 需处理：查看“需原文 / 仅摘要 / 需修复”的条目。
- 全选当前列表：只勾选当前搜索与筛选结果。
- PDF：使用系统默认阅读器打开附件。
- 生成 / 重新生成：生成一篇；批量按钮并发处理所选条目。

状态含义：

| 状态 | 含义 |
|---|---|
| 未生成 | 尚无对应笔记 |
| 已生成 | 笔记完整 |
| 仅摘要 | PDF 正文不可用，依据 Zotero 摘要生成 |
| 需原文 | PDF 正文和摘要都不可用，只写入证据不足骨架 |
| 需修复 | 旧笔记正文含多个占位符，可重新生成 |
| 失败 | 本次模型调用、校验或文件提交失败，可重试 |

## 输出与保护

每篇笔记名称为 `<safe-citekey>-<zotero-key>.md`，图表在同一输出目录下的 `images/<safe-citekey>-<zotero-key>/`。这样即使两个 citekey 清理后相同，也不会互相覆盖。

程序通过 Markdown frontmatter 的 `zotero_key` 定位已有笔记。你可以在输出目录内改名或移动笔记；重新生成仍会写回同一个文件。若同一 key 存在两份笔记，程序会提示冲突，避免覆盖错误文件。

重新生成时，旧文件中的 `## 我的笔记` 和 `## 疑问` 会保留。正文与图片整体提交；任何图片缺失或切换失败都会撤销本次写入。

## 模板

支持以下占位符：

`{{zotero:title}}`、`{{zotero:authors}}`、`{{zotero:year}}`、`{{zotero:doi}}`、`{{zotero:url}}`、`{{zotero:zotero_key}}`、`{{zotero:tags}}`、`{{zotero:pdf}}`、`{{zotero:item_type}}`、`{{zotero:note_file}}`、`{{zotero:llm}}`。

模板文件不存在时会自动创建默认模板。顶部“模板”按钮会用系统默认编辑器打开当前模板。

## 配置文件

`config.json` 只有六个字段：

```json
{
  "notes_path": "",
  "template_path": "template.md",
  "llm_base_url": "https://api.deepseek.com/v1",
  "llm_api_key": "",
  "llm_model": "deepseek-chat",
  "llm_profile": ""
}
```

旧版的 Vault 路径与笔记子目录会在读取时合并成 `notes_path`；下次保存后，旧开关不会继续写入配置。API Key 是明文，切勿分享个人 `config.json`。

## 常见问题

- 刷新失败：确认 Zotero 已启动并开启本地通信。
- 搜不到无 PDF 文献：切换为“全部”。
- 显示“需原文”：PDF 可能是扫描件或附件路径无效；补充可提取文本的 PDF 后重新生成。
- 生成失败：查看底部错误信息，确认模型地址、Key 和网络，然后重试。
- 移动笔记后提示重复：输出目录及其子目录里存在相同 `zotero_key` 的两份 Markdown，需要人工保留正确的一份。
