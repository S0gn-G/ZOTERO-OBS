# ZotNotes 使用说明书

ZotNotes 是一个 **Zotero ↔ Obsidian 论文笔记工具**：列出 Zotero 文献库中的论文，勾选后自动生成 Markdown 深度笔记写入 Obsidian 仓库，并可选接入大模型（OpenAI 兼容接口）自动撰写摘要/方法/创新点/实验结论。

---

## 一、系统要求（使用前必备）

| 软件 | 要求 |
|---|---|
| **Zotero 7** | 已安装、正在运行；并开启本地 API：`设置 → 高级 → 勾选 "Allow other applications on this computer to communicate with Zotero"`（勾选后需重启 Zotero 生效） |
| **Obsidian** | 已有仓库（vault），程序只往里面写 `.md` 笔记，不依赖 Obsidian 运行 |
| **Better BibTeX 插件**（可选） | 装上后文献有稳定 citekey（如 `endreiS3CLIPVideoSuper2026`），笔记文件名更规范；不装程序会自动兜底生成 |
| **大模型接口**（可选） | OpenAI 兼容接口（DeepSeek/Kimi/GLM 等）的 Base URL、API Key、模型名。不填则只生成笔记框架模板 |

> 本软件是**绿色免安装版**：不依赖本机 Python 环境，下载单个 `ZotNotes.exe` 双击即用。

---

## 二、快速开始

1. 从项目 **GitHub Releases** 下载 `ZotNotes.exe`，放到任意文件夹（建议单独建一个文件夹）。
2. 双击运行。首次启动会自动在 exe 同目录生成 `config.json` 与 `template.md`。
3. 若配置正常，窗口会自动列出 Zotero 库里的论文（右侧徽章显示「已生成/未生成」）。
4. 勾选想生成笔记的论文 → 点「生成勾选的笔记」；或点某行右侧的「生成」单独生成。

---

## 三、首次配置（点窗口顶部「设置」）

| 字段 | 说明 |
|---|---|
| **API 地址** | Zotero 本地 API，默认 `http://127.0.0.1:23119/api/`，一般不用改 |
| **Vault 路径** | 你的 Obsidian 仓库文件夹绝对路径 |
| **笔记文件夹** | 笔记写入 vault 内的子文件夹，默认 `LiteratureNotes`（不存在会自动创建） |
| **模板文件** | 笔记渲染用的模板（.md），点「浏览…」可选；留空则用 exe 同目录的 `template.md` |
| **Base URL** | 大模型接口地址，例如 `https://api.deepseek.com/v1`（OpenAI 兼容，结尾到 `/v1` 或 `/v4` 层级） |
| **API Key** | 大模型接口的鉴权令牌（`sk-...`） |
| **模型名** | 服务商给的模型标识，如 `deepseek-chat`、`deepseek-v4-flash` |
| **启用 LLM** | 关闭则只生成模板框架，不调接口 |
| **覆盖已生成的笔记** | 开启后重复生成会重写旧笔记。**注意**：无论开关与否，旧笔记手写的「我的笔记」「疑问」分区都会保留，只覆盖大模型生成的部分 |
| **仅显示含 PDF 的文献** | 开启后隐藏视频/网页快照等无 PDF 条目 |
| **读取 PDF 正文** | 开启后生成前解析 PDF 全文并提取图表喂给大模型，摘要/方法更准（稍慢） |

改完点「保存」，回到主界面点「刷新」生效。

---

## 四、日常使用

- **刷新**：重新从 Zotero 读取文献列表（文献多时自动分页拉全，不会只显示前 100 篇）。
- **单篇生成**：点论文行右侧「生成」按钮 → 生成中变「生成中…」→ 完成变绿色「已生成」；失败变红色「失败」+ 按钮变「重试」，可直接重试。
- **批量生成**：勾选多篇 → 点底部「生成勾选的笔记」。并发最多 3 路同时生成，速度更快。
- **打开 PDF**：论文行右侧「PDF」按钮，用系统默认阅读器打开原文。
- **模板**：顶部「模板」按钮用系统默认程序打开模板文件编辑，保存后下次生成即生效。
- **笔记位置**：生成后可在 Obsidian 中打开 `Vault/笔记文件夹/citekey.md`。

**列表状态徽章**：

| 徽章 | 颜色 | 含义 | 怎么办 |
|---|---|---|---|
| 已生成 | 绿 | 笔记完整 | — |
| 需原文 | 橙 | PDF 正文读不出且无摘要，只生成骨架笔记 | 打开原文补充后**直接点「生成」重试** |
| 需修复 | 红 | 笔记正文含「待补充」占位符（坏笔记） | 点「生成」重写 |
| 未生成 | 灰 | 还没生成 | 点「生成」 |

**上次编辑时间**：已生成笔记的元信息行末尾显示「更新于 时间」，取自笔记文件最后修改时间——在 Obsidian 里手动编辑过就是编辑时间，从未编辑过就是生成时间。

**笔记标题与别名**：笔记文件名是 citekey（如 `kimImageSuperresolutionImage2024`），保证唯一且可作链接。frontmatter 里带 `aliases: [论文标题]`，Obsidian 文件树会显示论文标题为别名，也可用 `[[论文标题]]` 直接链接到笔记。

**手写区保护**：笔记里 `## 我的笔记`、`## 疑问` 两个分区是留给你手写的。无论重新生成多少次，手写内容都会被自动拼回新笔记，不会丢失。

**笔记内容（多阶段深度笔记）**：生成时会自动 ① 读取 PDF 正文、② 从 PDF 提取图表、③ 让大模型先做写作规划再撰写。正文分区包括：

| 分区 | 内容 |
|---|---|
| 核心信息 | 标题/作者/机构/DOI/链接/代码等元数据 |
| 原文摘要翻译 | 原始摘要的忠实中文翻译 |
| 创新点 | 3-5 条，每条说明解决什么问题、带来什么新能力 |
| 一句话总结 | 论文定位的一句话 |
| 方法主线 | 含「机制流程」编号步骤 + 数据来源/训练细节等 |
| 核心实验 | 关键数值 + Markdown 对比表格（3 组以上对比时） |
| 局限 | 作者承认的 + 读者能观察到的局限 |
| 与我的研究的关系 | 与 SR/ReID 的交叉点、可复用思路 |
| 我的笔记 / 疑问 | 手动填写（自动保留） |

**图表**：从 PDF 提取的图片保存在 `Vault/笔记文件夹/images/citekey/` 下，笔记正文用相对路径引用，Obsidian 中可直接显示。

**关键公式**：方法涉及目标函数/更新规则时会用 `$...$` / `$$...$$` 数学格式渲染。

**Obsidian 渲染约束（生成时自动遵守）**：
- 表格单元格内**不用** `$...$` 行内公式（Obsidian 表格内公式渲染异常），单元格符号用纯文本或反引号（如 `I_SR`）。
- 每个表格各行列数一致。
- 标题层级只用 `##`（章节）与 `###`（小节）。

程序在生成后会自动校验这些约束（以及必需章节、图表引用、占位符），不满足时让大模型自动修正一轮。

---

## 五、笔记模板占位符

模板文件支持以下占位符（带 `zotero:` 前缀，避免与 Obsidian 内置模板语法冲突）：

| 占位符 | 含义 |
|---|---|
| `{{zotero:title}}` | 标题 |
| `{{zotero:authors}}` | 作者（逗号分隔） |
| `{{zotero:year}}` | 年份 |
| `{{zotero:doi}}` | DOI |
| `{{zotero:url}}` | 原文链接 |
| `{{zotero:zotero_key}}` | Zotero 条目 key |
| `{{zotero:tags}}` | 标签列表 |
| `{{zotero:pdf}}` | PDF 绝对路径 |
| `{{zotero:item_type}}` | 文献类型 |
| `{{zotero:note_file}}` | 笔记文件名 |
| `{{zotero:llm}}` | 大模型生成的多阶段深度笔记正文（未启用或失败时为空白） |

模板路径不存在时，程序会自动创建一份默认模板。

---

## 六、配置文件 `config.json`

程序同目录下的 `config.json` 保存所有设置（与「设置」窗口一一对应，改了窗口会写回这里）。字段：

`zotero_base` / `vault_path` / `notes_folder` / `template_path` / `llm_base_url` / `llm_api_key` / `llm_model` / `llm_enabled` / `overwrite` / `only_with_pdf` / `use_pdf_text`

> **安全提示：`config.json` 里 `llm_api_key` 是明文存储的。** 不要把整个文件夹（含该文件）分享给他人；拷贝给别人前建议把 `llm_api_key` 清空（留空会让程序在生成时提示填写）。

---

## 七、常见问题

**1. 打开后列表是空的 / 显示「读取失败」**
- Zotero 是否已启动？本地 API 是否开启（见第一节）？
- 如果刚勾选设置，需**重启 Zotero** 才生效。
- 「仅显示含 PDF 的文献」开启时，无 PDF 的条目会被隐藏（可在设置里关掉）。

**2. 点生成提示「LLM 已启用但未填 API Key」**
- 在设置里填写 API Key，或关闭「启用 LLM」。

**3. 生成失败 / 按钮变「重试」**
- 网络、代理或接口限流。程序对接口超时已做自动重试；可稍后点「重试」。
- 若本机走代理，偶尔首次连接慢属正常。
- 生成是「多阶段」的（先规划再写作再校验），比单次生成慢是正常的；每篇约需 2-3 次模型调用。

**4. 生成的摘要内容空/不全，或状态变橙色「需原文」**
- 检查是否开启「读取 PDF 正文」；扫描版 PDF（无文字层）读不出来，属正常。
- 可检查 `config.json` 里 `use_pdf_text` 是否为 `true`。
- 状态「需原文」= PDF 正文读不出且 Zotero 里没存摘要，程序按"不硬编"原则只生成骨架笔记，正文分区留空。
  打开原文补充后**直接点「生成」重试**即可，无需开启覆盖。
- 若笔记含「待补充」字样，可在生成后点「重试」让模型重新生成一轮。

**5. 我手写的「我的笔记」被覆盖了？**
- 不会。重新生成只覆盖大模型正文，「我的笔记」「疑问」手写区会自动拼回。

**6. 笔记里的表格在 Obsidian 中显示不对 / 列对不齐？**
- 程序已约束表格单元格不用 `$...$` 公式且各行列数一致。若旧笔记仍异常，重新生成一轮即可修复。
- 若表格上方没有空行，Obsidian 可能不把它当表格渲染（这是 Obsidian 解析规则，生成时已遵守）。

**7. 换了台电脑 / 想迁移**
- 下载 `ZotNotes.exe`，双击运行即可，无需安装 Python。
- 首次启动会生成 `config.json`；改里面的 `vault_path` 为目标机 Obsidian 仓库路径，LLM 的 Base URL / Key / 模型用目标机自己的账号。
- 旧机器的 `template.md`（自定义过的话）可拷到新目录覆盖。

---

## 八、文件构成（单文件应用）

```
你的文件夹/
├── ZotNotes.exe       主程序（从 Releases 下载，双击运行）
├── config.json        配置文件（首次启动自动生成，可改，见第六节）
├── template.md        笔记模板（首次启动自动生成，可改，见第五节）
└── (可选) theme.json  界面主题配色：放在 exe 同目录可覆盖内置配色
```

> 请把 `ZotNotes.exe` 单独放一个文件夹，`config.json`、`template.md` 都会生成在它旁边。
