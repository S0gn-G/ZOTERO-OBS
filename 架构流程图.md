# ZotNotes 当前架构

## 总体数据流

```mermaid
flowchart LR
    GUI["CustomTkinter GUI<br/>搜索、筛选、选择、状态"]
    Z["Zotero 本地 API<br/>127.0.0.1:23119/api/"]
    P["生成管道<br/>PDF 文本与图表 → LLM → lint"]
    W["ObsidianWriter<br/>定位、合并、事务提交"]
    O["notes_path<br/>Markdown + images/"]
    C["config.json<br/>六项设置"]

    GUI --> Z
    GUI --> P
    P --> W
    W --> O
    C --> GUI
```

## 刷新与筛选

```mermaid
flowchart TD
    A["刷新"] --> B{"当前忙碌?"}
    B -- 是 --> X["忽略重复请求"]
    B -- 否 --> C["锁定会改变状态的控件"]
    C --> D["工作线程分页读取 Zotero"]
    C --> E["递归扫描 notes_path 下的 Markdown"]
    E --> F{"同一 zotero_key 有多份?"}
    F -- 是 --> G["明确报冲突"]
    F -- 否 --> H["得到状态与修改时间"]
    D --> I["主线程更新文献数据"]
    H --> I
    I --> J["本地搜索与四种筛选视图"]
    J --> K["解锁控件"]
```

刷新直接保存完整文献列表；“含 PDF / 全部 / 未生成 / 需处理”只改变 GUI 显示，不触发新的网络请求，也不需要配置开关。

## 生成管道

```mermaid
flowchart TD
    A["生成一篇"] --> B{"有 PDF 正文?"}
    B -- 是 --> C["提取正文与图表"]
    B -- 否 --> D{"有 Zotero 摘要?"}
    D -- 否 --> E["写入 evidence: insufficient 骨架<br/>状态：需原文"]
    D -- 是 --> F["使用摘要证据<br/>状态：仅摘要"]
    C --> G["构建证据包"]
    F --> G
    G --> H["LLM 规划"]
    H --> I["LLM 写作"]
    I --> J["lint"]
    J --> K{"存在问题?"}
    K -- 是 --> L["LLM 修复一次"]
    L --> M["再次 lint"]
    K -- 否 --> N["渲染模板"]
    M --> O{"仍有问题?"}
    O -- 是 --> X["失败，不提交"]
    O -- 否 --> N
    N --> P["commit_generation"]
```

所有 lint 问题在修复后都视为失败条件；没有按问题类型再加一层复杂分支。

## 笔记与图片整体提交

```mermaid
flowchart TD
    A["content + figures"] --> B["按 zotero_key 定位已有笔记<br/>支持子目录内改名/移动"]
    B --> C["合并 我的笔记 / 疑问"]
    C --> D["图片复制到 images/stem.tmp"]
    D --> E{"图片完整?"}
    E -- 否 --> X["删除 tmp，不改正式文件"]
    E -- 是 --> F["Markdown 原子写入"]
    F --> G["旧图片 → .old<br/>.tmp → 正式图片目录"]
    G --> H{"切换成功?"}
    H -- 否 --> I["恢复旧 Markdown 与旧图片"]
    H -- 是 --> J["删除 .old，完成"]
```

## 模块职责

| 模块 | 单一职责 |
|---|---|
| `gui/app.py` | 展示、筛选、任务调度和线程安全 UI 更新 |
| `gui/settings_view.py` | 六项设置的编辑 |
| `zotero_client.py` | Zotero 分页读取、元数据标准化和 PDF 路径解析 |
| `note_generator.py` | 证据提取、模型调用、校验与模板渲染 |
| `obsidian_writer.py` | 笔记定位、手写区合并和事务提交 |
| `config.py` | 默认值、旧版迁移、过滤和原子保存 |

批量生成由一个工作线程管理 `ThreadPoolExecutor(max_workers=3)`；所有 Tk 控件更新通过 UI 队列回到主线程。设置、刷新和生成在同一时间只允许一个状态性操作，避免旧刷新结果覆盖新配置。
