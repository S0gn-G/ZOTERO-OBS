# ZotNotes 架构流程图

> 本图由源码绘制：`gui/app.py` + `note_generator.py` + `zotero_client.py` + `obsidian_writer.py` + `config.py`。
> Obsidian 中以代码块渲染（mermaid 原生支持）。

## 一、总体分层

```mermaid
flowchart LR
    subgraph GUI["GUI 层 · gui/app.py"]
        direction TB
        W1["App 主窗口"]
        W2["PaperRow 卡片"]
        W3["SettingsWindow 设置窗 / 模板窗"]
        W4["线程安全 UI 调度<br/>_post → _ui_queue → _poll_ui_queue"]
    end

    subgraph ZC["zotero_client.py"]
        direction TB
        Z1["fetch_papers 翻页<br/>start + limit=100 + Total-Results 头"]
        Z2["_find_pdf 附件定位<br/>_pdf_path 解析 file:// 路径"]
    end

    subgraph NG["note_generator.py 多段管道"]
        direction TB
        N1["Stage1 正文提取"]
        N2["Stage2 图表提取"]
        N3["Stage3 证据包"]
        N4["Stage4 LLM 计划"]
        N5["Stage5 LLM 写作"]
        N6["Stage6 lint + 修复"]
        N1 --> N2 --> N3 --> N4 --> N5 --> N6
    end

    subgraph OW["obsidian_writer.py"]
        direction TB
        O1["scan_states 三态检测"]
        O2["write_note_preserving<br/>merge_handwritten 合并手写区"]
        O3["import_images 拷图"]
    end

    subgraph CFG["config.py"]
        direction TB
        CF1["config.json 读写"]
        CF2["template.md 模板<br/>BASE_DIR 依 frozen/exe 分支"]
    end

    GUI -->|论文列表| ZC
    GUI -->|生成请求| NG
    NG -->|content + figures| OW
    GUI <--> CFG
    OW <--> V["Obsidian vault<br/>REID/LiteratureNotes"]
    ZC <--> Z["Zotero 本地 API<br/>127.0.0.1:23119"]
    NG <--> L["LLM OpenAI 兼容<br/>deepseek"]
```

## 二、刷新流程

```mermaid
flowchart TD
    A["用户点「刷新」"] --> B{"_busy 忙?"}
    B -- 是 --> E1["直接返回"]
    B -- 否 --> C["启动线程 _refresh_worker"]
    C --> D["fetch_papers<br/>翻页拉全（三重终止）"]
    C --> E["scan_states<br/>单次扫描笔记目录，读全文"]
    D --> F["论文列表 papers<br/>按 only_with_pdf 过滤"]
    E --> G["(key, state, mtime) 列表<br/>state ∈ ok / insufficient / placeholder"]
    F --> H["主线程 _apply_refresh"]
    G --> H
    H --> I["重建列表行<br/>state 映射优先级：<br/>placeholder > insufficient > ok > none"]
```

## 三、生成流程（单篇 / 批量）

```mermaid
flowchart TD
    A["勾选 + 点「生成勾选的笔记」"] --> B{"至少勾选一篇?"}
    B -- 否 --> E1["提示：请先勾选"]
    B -- 是 --> C["筛选 to_process<br/>overwrite 或 note_state != ok"]
    C --> D["ThreadPoolExecutor<br/>MAX_WORKERS=3"]
    D -->|每篇| R["_run_generate(row, writer)"]
    R --> P1["generate_note 多段管道"]
    R --> P2["write_note_preserving<br/>merge_handwritten 拼旧手写区"]
    R --> P3["import_images<br/>暂存图 → vault/images/key/"]
    R --> P4["cleanup_figures 删暂存目录"]
    P1 --> RES["(ok, status, msg)"]
    P2 --> RES
    P3 --> RES
    P4 --> RES
    RES --> F["主线程 _finish_generate"]
    F --> G["按 status 标记行<br/>ok 已生成✔ / needs_source 需原文⚠ / failed 失败✗"]
```

## 四、多段生成管道（generate_note 核心）

```mermaid
flowchart TD
    A["generate_note(paper, cfg, note_key)"] --> B{"llm_enabled?"}
    B -- 否 --> R0["render_note 模板替换"]
    R0 --> O0["(content, figures: 空, status: ok)"]

    B -- 是 --> C{"use_pdf_text?"}
    C -- 是 --> D["Stage1 正文提取 pdfplumber<br/>截断 30000 字符"]
    D --> E["Stage2 图表提取 PyMuPDF<br/>按图注定位渲染 → %TEMP%/zotnotes_fig_*"]
    E --> G
    C -- 否 --> G{"有证据?<br/>pdf_text 或 abstract 非空"}
    G -- 否 --> S["render_skeleton 骨架<br/>frontmatter 标 evidence: insufficient"]
    S --> O1["(content, figures: 空, status: needs_source)"]

    G -- 是 --> H["Stage3 证据包 bundle JSON<br/>meta + 摘要 + 正文 + 图表清单"]
    H --> I["Stage4 llm_plan<br/>计划 JSON · 容错重试 x3"]
    I --> J["Stage5 llm_write<br/>整篇正文 Markdown ≥800 字符 · 重试 x3"]
    J --> K["Stage6 lint_note<br/>缺章节 / 占位符 / 图表引用 / 表格公式 / 列数 / 标题层级"]
    K --> L{"有 issues?"}
    L -- 是 --> M["llm_fix 按清单修正 · 重试 x2"]
    M --> K2["二次 lint"]
    K2 --> L2{"fatal?<br/>缺必需章节 / 占位符"}
    L -- 否 --> N["_substitute 填充 frontmatter"]
    L2 -- 是 --> ERR["抛异常 → cleanup_figures → 上抛"]
    L2 -- 否 --> N
    N --> O2["(content, figures, status: ok)"]
```

## 五、笔记三态检测（scan_states）

```mermaid
flowchart TD
    A["读笔记全文"] --> B["提取 frontmatter zotero_key"]
    B --> C{"有 key?"}
    C -- 否 --> D["跳过该文件"]
    C -- 是 --> E["分割：llm_body = 内容[到「## 我的笔记」]"]
    E --> F{"llm_body 含 ≥2 处<br/>待补充 / TODO / TBD?"}
    F -- 是 --> G["placeholder 需修复 · 红✗"]
    F -- 否 --> H{"全文含 evidence: insufficient?"}
    H -- 是 --> I["insufficient 需原文 · 琥珀⚠"]
    H -- 否 --> J["ok 已生成 · 绿✔"]
```

## 六、关键设计点

| 设计 | 说明 |
|---|---|
| 高内聚低耦合 | 管道是纯函数链（`generate_note` 不碰 GUI）；`merge_handwritten` 是纯函数，GUI 只换一行调用；`scan_states` 单次扫描，4 个查询方法都是它的派生 |
| 线程模型 | 所有 I/O（Zotero / LLM / 文件）在工作线程；UI 更新一律 `_post` 排队到主线程，避免 tkinter 跨线程崩溃 |
| fail-closed | 无证据不硬编：生成骨架 + 标记，GUI 显示「需原文」；补原文后可重试，不会被 overwrite 拦截 |
| 失败清理 | LLM 阶段异常 `cleanup_figures` 清暂存；成功路径由 GUI 拷贝完再清；空暂存目录当场删 |
| 覆盖保护 | 重新生成不覆盖手写：`write_note_preserving` 把旧笔记的「我的笔记 / 疑问」拼进新正文 |
