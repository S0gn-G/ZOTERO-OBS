# 修复计划 3 — review2 落地实施

## Context

review2.md 针对最新提交 `5158de0`，结论：主体设计已基本可用，但有 **2 个 P0 数据一致性漏洞 + 3 个 P1** 需先堵住。
已逐条核实并隔离复现：citekey 碰撞导致覆盖他人笔记、图片导入部分失败污染旧图、lint 用原始 key 误报正确路径均确认属实。

本轮只做一轮集中修复，不改架构。改 3 个源码文件 + 测试。

## 修复项总览

| # | 优先级 | 位置 | 修复 |
|---|---|---|---|
| 1 | P0 | obsidian_writer.py | 文件名唯一化 `<safe citekey>-<zotero key>`，杜绝 sanitize 碰撞 |
| 2 | P0 | obsidian_writer.py | 图片导入事务化：先写 `.tmp`，全部成功再一次性切换 |
| 3 | P1 | note_generator.py | `lint_note` / `note_file` / prompt 统一用同一 canonical 目录 |
| 4 | P1 | note_generator.py | Figure/Table 按类型分方向裁剪（Table 优先向下） |
| 5 | P1 | note_generator.py | 图注间允许合理 gap，高图不再截顶 |
| 6 | P2 | obsidian_writer.py | Windows 保留设备名（CON/NUL/COM1…）处理 |
| 7 | P2 | 全部 | 补齐碰撞/事务失败/几何/保留名 回归测试 |

## 一、obsidian_writer.py

### 1a（P0#1）canonical 唯一名 + 目标文件 zotero_key 校验

```python
WINDOWS_RESERVED = ({"CON", "PRN", "AUX", "NUL"}
                    | {f"COM{i}" for i in range(1, 10)}
                    | {f"LPT{i}" for i in range(1, 10)})


def safe_citekey(key, fallback="note"):
    s = (key or "").strip()
    s = re.sub(r"[^A-Za-z0-9_.\-]", "_", s)
    s = re.sub(r"\.{2,}", "_", s)
    s = s.strip("._-") or fallback
    if s.split(".", 1)[0].upper() in WINDOWS_RESERVED:
        s = "_" + s  # 保留设备名即使带扩展名也非法（CON.md / NUL.txt）
    return s[:80]


def note_stem(citation_key, zotero_key):
    """唯一文件名/图片目录片段：<safe citekey>-<zotero key>。
    zotero key 全局稳定唯一，即使 sanitize 碰撞也不会互相覆盖。"""
    return f"{safe_citekey(citation_key)}-{zotero_key}"
```

`ObsidianWriter` 增 `_stem(citation_key, zotero_key=None)`（有 key 用 `note_stem`，否则退 `safe_citekey` 兼容旧调用）；
`note_path`/`import_images`/`write_note`/`write_note_preserving` 全部改走 `_stem`。
`write_note_preserving`：目标存在时先读其 frontmatter `zotero_key`，与当前不一致则视为占用（异常态），
不继承其手写区；仍按 zotero_key 精确查找本文旧笔记。

### 1b（P0#2）事务式导入 + 原子写

```python
@staticmethod
def _atomic_write(path, content):
    tmp = path + ".tmp"
    with open(tmp, "w", encoding="utf-8", newline="\n") as f:
        f.write(content)
    os.replace(tmp, path)
```

`import_images` 改为事务式：
1. 全图先拷入 `images/<stem>.tmp/`（先清残留）；任一图缺失/失败 → 清理 tmp、正式目录不动，返回 missing。
2. 全部成功 → 旧目录改名 `.old`，`os.replace(tmp → 正式)`，删 `.old`；切换失败回滚 `.old`。
3. 返回 missing（空 = 已提交）。

`write_note` / `write_note_preserving` 用 `_atomic_write` 落盘。

## 二、note_generator.py

### 2a（P1#3）canonical 目录贯通
- `generate_note` 开头算 `stem = note_stem(note_key, paper["key"])`；
  `note_file = stem + ".md"`（修 P2 note_file 假值）。
- `_extract_figures(pdf, stem)`：暂存前缀用 stem。
- `_write_user(bundle, plan, image_dir)`：prompt 用 `images/{image_dir}/`；`llm_write` 末参改名 `image_dir` 并透传。
- `lint_note(body, plan, figures, image_dir)`：`wanted`/校验全部用 `images/{image_dir}/...`。
- `WRITE_SYSTEM_TMPL` 里静态示例 `images/<note_key>/` 改为 `images/<image_dir>/`。

### 2b（P1#4/#5）按类型分方向裁剪 + 允许 gap
新增纯函数（可单测）：
```python
FIG_CAPTION_MAX_GAP = 60   # 图/表与其 caption 最大空隙
FIG_MAX_HEIGHT = 360       # fig 向上最多找多高
MARGIN_BOTTOM = 40.0

def _clip_region(cap, next_cap, image_rects, prev_bottom, margin_top, page_w, page_h):
    # fig：默认裁 caption 上方；table：默认裁 caption 下方
    # 图/表是光栅图时，用内嵌图片 bbox 在 gap 内上探/下探修正
```
`_extract_figures` 主循环改为逐 caption 调 `_clip_region`；table 处理后 `prev_bottom = max(prev_bottom, y1)`，
fig 后 `prev_bottom = cap["y1"]`。

### 2c（P2）lint fail-open 收紧
`generate_note` 里 fatal 集合加入 `"图表引用指向不存在的文件"`（修复后目录唯一，正确引用不应报错；
坏链是内容损坏，应失败而非静默保存）。

## 三、gui/app.py

`_run_generate` 的 `import_images` 补传 `zotero_key=row.paper["key"]`（`write_note_preserving` 已传）。

## 四、测试

- `tests/test_path.py`：保留设备名（CON/NUL/COM1→前缀 _）、`note_stem` 唯一性。
- `tests/test_writer.py`：更新 canonical 路径断言；新增 **碰撞不覆盖**（K1/K2 同 safe 名 → 两文件、互不继承手写）；
  新增 **导入事务失败不污染**（fig_2 缺失 → 正式目录保持旧图）、**导入成功替换旧图**。
- `tests/test_lint.py`：`lint_note` 换参名 image_dir；新增 **用 canonical 目录不误报**。
- `tests/test_evidence.py`：新增特殊字符 citekey（`Wang&Li,2022`）下正文引用 `images/Wang_Li_2022-K1/...` 不触发 lint 错误。
- 新增 `tests/test_figures.py`：`_clip_region` — fig 上探/高图 gap/table 向下/table 截到 next caption/prev_bottom 约束。

## 实施顺序

1. obsidian_writer.py（1a → 1b）
2. note_generator.py（2a → 2b → 2c）
3. gui/app.py（三）
4. 测试更新（四）

## 验证

1. `python -m py_compile` 全部文件。
2. `python -m pytest tests/` 全绿（原 39 + 新增）。
3. 隔离模拟：K1/A&B 与 K2/A B 各生成 → 两个独立文件、手写区各自独立；
   `import_images` 一半成功一半失败 → 正式目录图像不变。
4. 真实 Zotero 未运行时跳过 live 脚本（test_core/test_pipeline）；用户有空再跑。
5. 手动 GUI 冒烟（可选）：生成一篇含特殊字符 citekey 的论文，检查 frontmatter `note_file`、
   图片路径与 lint 一致，`images/<stem>/` 目录唯一。
6. 重新推送 GitHub（exe 不重打包，源码稳定后单独做）。
