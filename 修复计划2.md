# ZotNotes 第二轮修复计划（review1.md 落地）

## Context

针对 `review1.md` 的静态审查 + 隔离模拟结论，逐条核实后**全部属实**。本次按「边界与事务优先」顺序修复：先 P0（路径约束 / 原子交付 / 改名笔记 / 首启配置），再 P1（附件索引 / 配置快照 / 图表提取），最后 P2（证据等级 / LLM 校验 / 领域 profile / 测试工程化）。不重构整体分层，改动集中在各模块边界处。exe 重新打包与 Release 更新在代码稳定后进行。

## 修复项总览

| # | 优先级 | 问题 | 修复位置 |
|---|---|---|---|
| 1 | P0 | citekey 无文件名规范化 | obsidian_writer.py `safe_citekey` + note_path/import_images/staging 全链路 |
| 2 | P0 | 图片导入失败被吞、交付非原子 | obsidian_writer `import_images` 返回缺失清单；app.py `_run_generate` 图先行 + finally 清暂存 |
| 3 | P0 | 改名笔记识别≠可覆盖合并 | obsidian_writer 按 zotero_key 找旧文件，写入旧路径保留用户改名 |
| 4 | P0 | 首启写死开发者 vault 路径 | config.py DEFAULT_CONFIG vault_path→"" + GUI 空 vault 提示 + 生成前拦截 |
| 5 | P1 | 附件定位 O(N²) | zotero_client fetch_papers 建 parentItem→attachment 索引，O(N) |
| 6 | P1 | 批量期间配置分裂 | app.py 生成时冻结 cfg 快照；_set_busy 禁用设置按钮 |
| 7 | P1 | 图表裁剪裁掉宽图 / 只识别 Figure | note_generator 横向放宽到正文宽度 + 图片区域上探 + 支持 Table 图注 |
| 8 | P2 | 证据等级太粗 | note_generator fulltext/abstract_only/none 三级 + 新增 GUI 状态「仅摘要」 |
| 9 | P2 | LLM plan schema 弱 / response_format 强假设 / fix 无证据 | _validate_plan + response_format 回退 + llm_fix 携带 bundle |
| 10 | P2 | 领域 profile 硬编码 | config 增加 llm_profile，prompt 模板化，设置窗口可编辑 |
| 11 | P2 | 测试无断言、依赖不锁版 | 新增 tests/ 单元测试（无需真 Zotero/LLM）；requirements 拆 runtime/dev 并锁版本 |

---

## 一、P0

### 1a 文件名安全 `safe_citekey`（Bug#1）

新增模块级函数（obsidian_writer.py，文件系统边界的属主）：

```python
def safe_citekey(key: str, fallback: str = "note") -> str:
    s = (key or "").strip()
    s = re.sub(r"[^A-Za-z0-9_.\-]", "_", s)   # 只留安全字符
    s = re.sub(r"\.{2,}", "_", s)             # 折叠 ..
    s = s.strip("._-") or fallback
    return s[:80]
```

- `note_path()`、`import_images()` 内部先 `citation_key = safe_citekey(citation_key)`。
- note_generator `_extract_figures` 的暂存前缀与 `_write_user` 提示中的 `images/<key>/` 同样走 `safe_citekey`（note_generator 从 obsidian_writer import）。
- app.py 生成行时 `note_key = safe_citekey(citekey)`，全链路单点一致。
- 效果：`../escape` → `_escape`，不再越出目录；Windows 保留字符被替换。

### 1b 原子交付：图片缺失 = 失败（Bug#2）

- `import_images` 返回值从 `written 列表` 改为 `missing 文件名列表`（无图返回 `[]`）：
  - 源不存在 / copy 抛 OSError / dst 写入失败 → 计入 missing，`continue`。
- app.py `_run_generate` 改为「图先行 + 事后必清」：

```python
def _run_generate(self, row, writer, cfg):
    figures = []
    try:
        result = generate_note(row.paper, cfg, note_key=row.note_key)
        figures = result["figures"]
        if figures:
            missing = writer.import_images(row.note_key, figures)
            if missing:
                return False, "failed", f"图表拷贝失败：{', '.join(missing)}"
        writer.write_note_preserving(row.note_key, result["content"], zotero_key=row.paper["key"])
        return True, result["status"], "OK"
    except Exception as e:
        return False, "failed", str(e)
    finally:
        if figures:
            cleanup_figures(figures)   # 无论成败都不留暂存
```

- 语义：正文引用必须指向真实落盘的图，否则整篇判失败（不产生「已生成但缺图」状态）。

### 1c 改名笔记：按 zotero_key 定位旧文件（Bug#3）

- 新增 `ObsidianWriter._find_note_by_key(zotero_key) -> str | None`：扫描 notes 目录，返回 frontmatter `zotero_key` 匹配的文件路径（只读每文件头部 ~500B，便宜）。
- `write_note_preserving(citation_key, content, zotero_key=None)`：
  1. 规范路径存在 → 旧文件 = 规范路径；
  2. 否则 zotero_key 提供 → `_find_note_by_key`；
  3. 旧内容合并手写区后，**写入旧文件所在路径**（保留用户改名，不产生第二份）。
- app.py 调用处传入 `zotero_key=row.paper["key"]`。

### 1d 首启配置不再泄漏开发机路径（Bug#4）

- `DEFAULT_CONFIG["vault_path"]` → `""`。
- GUI：`App.__init__` 首启落盘后若 `vault_path` 为空 → 状态栏提示「请在 设置 中填写 Vault 路径」；`generate_one`/`generate` 前检查空 vault 直接拦截提示。
- `config.example.json` 已为空，行为一致。

---

## 二、P1

### 2a PDF 附件索引 O(N)（Bug#5）

- `fetch_papers` 拉完全部 items 后一次性建索引：

```python
att_by_parent: dict[str, dict] = {}
for it in items:
    d = it.get("data", {})
    if (d.get("itemType") == "attachment" and d.get("contentType") == "application/pdf"
            and d.get("parentItem")):
        att_by_parent.setdefault(d["parentItem"], it)
```

- `_find_pdf(it, att_by_parent)` → `att_by_parent.get(it["key"])`。`_to_paper` 传索引而非 by_key。
- 复杂度 O(N²) → O(N)（500→1500 规模从 ~0.18s 降到线性）。

### 2b 批量配置快照 + busy 禁设置（Bug#6）

- `generate_one`/`generate` 开始时 `cfg_snapshot = dict(self.cfg)`，线程任务一律用快照（`_run_generate(row, writer, cfg_snapshot)`），writer 也从快照的 vault 创建。
- `_set_busy(True)` 时同时禁用顶部「设置」按钮（`self.settings_btn`，_build_ui 里保存引用），杜绝批量中途改配置。

### 2c 图表提取：宽图 + Table 图注（Bug#7）

- 横向裁剪放宽到正文整页宽度（图注宽度≠图宽）：`x0 = margin_left; x1 = page_w - margin_left`。
- 纵向：对当前图注上方、横向重叠的页面内嵌图片区域（`page.get_image_rects`），上探到该区域顶边；无则保留向上 320pt 兜底。
- 图注正则同时识别 `Figure/Fig./Table/Tab.`，文件名区分 `fig_N_pX.png` / `table_N_pX.png`。
- 多行图注：`_page_captions` 由「仅首行」改为拼接 block 内所有行 spans（保留首行匹配判定）。

---

## 三、P2

### 3a 证据等级三级（Bug#8）

- `generate_note`：

```python
if pdf_text.strip():
    evidence = "fulltext"
elif (paper.get("abstract") or "").strip():
    evidence = "abstract_only"
else:
    evidence = "none"
```

- `none` → 骨架 + status `needs_source`（现状不变）。
- `abstract_only` → 正常走管道，但最终 content 的 frontmatter 插入 `evidence: abstract_only`，status 返回 `abstract_only`。
- obsidian_writer `scan_states` 新增 `EVIDENCE_ABSTRACT_RE` → state `abstract_only`（优先级 placeholder > insufficient > abstract_only > ok）。
- GUI：`NOTE_STATE_LABEL["abstract_only"]="仅摘要"`（琥珀色），`PaperRow.mark_abstract_only()`，`_apply_refresh`/`_finish_one`/`_finish_generate` 补分支；`generate_one` 的 overwrite 拦截对 abstract_only 放行（可重试）。
- 语义：仅摘要生成的笔记与读全正文的笔记用不同状态区分，避免误导。

### 3b LLM 计划校验 + 兼容回退 + 修复带证据（Bug#9）

- 新增 `_validate_plan(plan) -> list[str]`：`paper_type ∈ 白名单`，`figures_to_reference/required_sections/mechanism_flow/key_numbers/claim_boundaries/limitations/followup_questions` 为数组。`llm_plan` 在 `isinstance dict` 后校验，非法 → 记错误并重试。
- `llm_plan` 的 `response_format={"type":"json_object"}` 失败时**回退为不带该参数的重试**（兼容部分接口不支持 json_object）。
- `llm_fix(body, issues, cfg, bundle=None)`：传入证据包，修复轮 prompt 中附上原文证据，约束修复不得脱离事实。

### 3c 领域 profile 可配置（Bug#10）

- config 增加 `"llm_profile": ""`（空 → 默认 SR/ReID 研究员 profile）。
- `PLAN_SYSTEM`/`WRITE_SYSTEM` 改为模板字符串，开头注入 profile：`cfg.get("llm_profile") or DEFAULT_DOMAIN_PROFILE`。
- settings_view 增加「领域 profile」多行文本框（config 可覆盖），保存写回。
- 效果：工具可服务任意研究领域，默认仍贴合作者自己的 SR/ReID 流程。

### 3d 测试工程化（Bug#11）

- 新增 `tests/` 纯单元测试（不依赖真 Zotero/LLM/PDF）：
  - `test_path.py`：safe_citekey（`../`、保留字符、空值、长度）。
  - `test_writer.py`：merge_handwritten 全分支、write_note_preserving 改名文件合并、scan_states 四态。
  - `test_lint.py`：lint_note 各检查 + 代码块不误报 + `_validate_plan`。
  - `test_zotero.py`：mock requests，分页拉全/终止条件/附件索引 O(N)。
  - `test_evidence.py`：证据等级 none/abstract_only/fulltext 分支。
- `requirements.txt` 锁 runtime 版本（customtkinter/openai/requests/pdfplumber/pymupdf 按 venv 实测版本）；新增 `requirements-dev.txt`（pytest、pyinstaller==6.21.0）。
- README 补「运行测试」段。

---

## 实施顺序

1. obsidian_writer.py（1a→1b→1c→scan_states 四态）——先就位 `safe_citekey`/`import_images` 返回值
2. zotero_client.py（2a）
3. note_generator.py（1a 引用 →2c→3a→3b→3c）
4. config.py + settings_view.py（1d→3c 配置）
5. gui/app.py（1d 提示→2b→1b 调用→四态 UI）
6. tests/ + requirements 拆分（3d）
7. 写本文档副本至 zotnotes_tool

## 验证

1. `python -m py_compile` 全部改动文件。
2. `pytest tests/ -v`（mock 数据，无外网依赖）。
3. `test_core.py` / `test_pipeline.py` 回归（需真 Zotero/LLM，可选）。
4. 针对性模拟：citekey=`../escape` 不再越目录；空 vault 生成被拦截；改名笔记重生成合并手写且不产生双份；图缺失时 _run_generate 返回失败；宽图（475pt）不再被右侧裁剪；abstract_only 笔记状态=仅摘要。
5. 重新打包 onefile exe → 首启行为回归（自动生成 config/template，vault_path 为空需用户填写）。
6. 全部通过后重新推送并更新 Release。
